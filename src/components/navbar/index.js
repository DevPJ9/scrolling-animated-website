import React from "react";
import { Nav } from "./NavbarElements";

// we named this folder index.js so that when we import this class we can just import till the folder name and it will autiomatically search index.js file

const Navbar = () => {
  return (
    <>
      <Nav>
        {/* <NavbarContainer>
          <NavLogo>dolla</NavLogo>
        </NavbarContainer> */}
      </Nav>
    </>
  );
};

export default Navbar;
